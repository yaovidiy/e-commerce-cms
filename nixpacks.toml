# Nixpacks configuration for Railway
# Auto-detects build dependencies for native modules

[providers]
nodejs = "20"

[phases.build]
cmds = ["bun install", "bun run build"]

[phases.start]
cmd = "bun run start"
