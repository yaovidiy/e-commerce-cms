# Nixpacks configuration for Railway
# Auto-detects build dependencies for native modules

[phases.build]
cmds = ["bun install", "NODE_OPTIONS=--no-warnings bun run build"]

[phases.start]
cmd = "bun run start"


