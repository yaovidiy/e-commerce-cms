# Nixpacks configuration for Railway
# Ensures all build dependencies are available for native modules

[phases.setup]
nixPkgs = ["python3", "pkg-config", "sqlite", "libpng", "libjpeg", "libvips"]

[phases.build]
cmds = ["bun install", "bun run build"]

[phases.start]
cmd = "bun run start"
